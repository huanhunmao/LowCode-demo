import { ModuleConfigs } from '@foxpage/foxpage-types';
export declare type FrameworkOptions = {
    requirejsLink: string;
    libs: ModuleConfigs;
    win?: Window;
};
export declare function loadFramework(libs: ModuleConfigs): Promise<any[]>;
export declare function initFramework(initialState: FrameworkOptions, opt?: {
    clearCache: boolean;
}): Promise<any[]>;
