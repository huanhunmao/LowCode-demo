import { Context, Mock, Page, Template } from '@foxpage/foxpage-types';
export interface MockOptions {
}
/**
 * parse page with mock
 * @param page
 * @param mock
 * @param extendMock
 * @returns
 */
export declare const mockPage: (page: Page, mock: Mock | null, extendMock: Mock | null) => Page;
/**
 * parse template with mock
 * @param template
 * @param mock
 * @returns
 */
export declare const mockTemplate: (template: Template, mock: Mock) => Template;
/**
 * with mock
 * merge mock data to contents
 * @param mocks
 * @param ctx
 * @param opt
 * @returns
 */
export declare const withMock: (mocks: Mock[], ctx: Context, _opt?: MockOptions | undefined) => {
    page: Page | undefined;
    templates: Template[] | undefined;
};
