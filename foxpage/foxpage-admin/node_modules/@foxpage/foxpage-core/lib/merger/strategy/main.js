Object.defineProperty(exports, "__esModule", { value: true });
exports.strategyMerge = void 0;
const match_1 = require("./match");
const merge_1 = require("./merge");
const type_1 = require("./type");
exports.strategyMerge = (record, list, strategy = type_1.MergeStrategy.COMBINE) => {
    const matcher = match_1.matcherCreator(strategy);
    if (typeof matcher === 'function') {
        const merger = merge_1.mergerCreator(strategy);
        if (typeof merger === 'function') {
            matcher(record, list, merger);
        }
    }
};
