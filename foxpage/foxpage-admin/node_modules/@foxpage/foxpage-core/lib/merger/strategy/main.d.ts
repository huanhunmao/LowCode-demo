import { MergeStructureNode } from '../interface';
import { MergeStrategy } from './type';
export declare const strategyMerge: <T extends MergeStructureNode>(record: Record<string, T>, list: T[], strategy?: MergeStrategy) => void;
