import { MessageArray } from '@foxpage/foxpage-types';
export interface IMessages extends MessageArray {
    hasError?: boolean;
    formate(includeStack?: boolean): string[];
    stringify(splitter?: string): string;
    toJSON(): string[];
}
export declare class Messages extends Array<string | Error> implements IMessages {
    get hasError(): boolean;
    formate(includeStack?: boolean): string[];
    stringify(splitter?: string): string;
    toJSON(): string[];
}
export declare function stringifyMessages(messages?: Array<string | Error>, splitter?: string): string;
export declare function formatMessages(messages?: Array<string | Error>, includeStack?: boolean): string[];
