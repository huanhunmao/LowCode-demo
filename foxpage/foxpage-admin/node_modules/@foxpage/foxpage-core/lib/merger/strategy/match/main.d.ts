import { MergeStrategy } from './../type';
export declare const matcherCreator: (strategy?: MergeStrategy) => (<T extends import("../../interface").MergeStructureNode>(record: Record<string, T>, list: T[], mergeNode: (base: T, current: T) => T) => void) | undefined;
