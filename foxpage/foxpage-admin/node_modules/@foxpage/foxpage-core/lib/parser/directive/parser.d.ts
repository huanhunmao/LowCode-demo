import { Messages } from '@foxpage/foxpage-shared';
import { Context, Directive } from '@foxpage/foxpage-types';
/**
 * directive parser
 *
 * @export
 * @class DirectiveParser
 */
export declare class DirectiveParser {
    private directives;
    constructor(data: Directive);
    hasIf(): boolean | undefined;
    hasTpl(): boolean;
    parseIf(ctx: Context, messages: Messages): boolean | undefined;
    parseTpl(ctx: Context, messages: Messages): unknown;
    private parseItem;
}
