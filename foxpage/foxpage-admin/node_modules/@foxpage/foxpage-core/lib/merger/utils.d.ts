/**
 * merge the any object
 * @param base base data
 * @param current current data
 * @returns merged data
 */
export declare const mergeObject: <P extends Record<string, any>>(base: P, current: P) => P;
