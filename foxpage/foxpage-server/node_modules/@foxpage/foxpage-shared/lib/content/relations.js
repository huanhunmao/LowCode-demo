Object.defineProperty(exports, "__esModule", { value: true });
exports.relationsMerge = void 0;
exports.relationsMerge = (relation, relations) => {
    Object.keys(relation).forEach(key => {
        const keyStr = key;
        if (relation[keyStr]) {
            if (!relations[keyStr]) {
                relations[keyStr] = [];
            }
            // @ts-ignore
            relations[keyStr] = (relations[keyStr] || []).concat(relation[keyStr]);
        }
    });
    return relations;
};
