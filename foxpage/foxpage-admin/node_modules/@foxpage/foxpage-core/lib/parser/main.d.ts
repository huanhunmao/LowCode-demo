import { Messages } from '@foxpage/foxpage-shared';
import { Context, Page, PageParser, TemplateParser } from '@foxpage/foxpage-types';
export declare class MainParser {
    /**
     * page parser
     *
     * @type {PageParser}
     */
    pageParser?: PageParser;
    /**
     * template parser
     *
     * @type {TemplateParser}
     */
    templateParser?: TemplateParser;
    /**
     * messages
     */
    messages?: Messages;
    constructor({ page }: {
        page: Page;
    }, ctx: Context);
    prepare(ctx: Context): void;
    parse(ctx: Context): Promise<{
        parsed: import("@foxpage/foxpage-types").StructureNode<any>[];
        messages: Messages | undefined;
    } | undefined>;
    reset(): void;
}
