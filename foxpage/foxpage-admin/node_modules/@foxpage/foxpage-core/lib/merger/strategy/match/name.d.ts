import { MergeStructureNode } from '../../interface';
/**
 * match the node by name, match all
 * @param record
 * @param list
 * @param mergeNode
 */
export declare const matchByName: <T extends MergeStructureNode>(record: Record<string, T>, list: T[], mergeNode: (base: T, current: T) => T) => void;
