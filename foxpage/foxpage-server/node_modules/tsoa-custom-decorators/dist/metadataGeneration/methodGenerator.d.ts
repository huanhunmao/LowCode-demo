import * as ts from 'typescript';
import { Method } from './metadataGenerator';
import { DecoratorsSchema } from './acceptedDecoratorsSchema';
export declare class MethodGenerator {
    private readonly node;
    private readonly decoratorsSchema;
    private method;
    private path;
    private decorators;
    constructor(node: ts.MethodDeclaration, decoratorsSchema: DecoratorsSchema);
    IsValid(): boolean;
    Generate(): Method;
    private buildParameters(acceptedParameterDecorators);
    private getCurrentLocation();
    private processMethodDecorators();
    private getMethodResponses();
    private getMethodSuccessResponse(type);
    private getMethodSuccessExamples();
    private supportsPathMethod(method);
    private getExamplesValue(argument);
    private getMethodTags();
    private getMethodSecurity();
    private getInitializerValue(initializer);
    private isAcceptMethodDecorator(key, decoratorName);
}
