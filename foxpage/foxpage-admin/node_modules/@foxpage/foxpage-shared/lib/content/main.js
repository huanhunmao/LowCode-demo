Object.defineProperty(exports, "__esModule", { value: true });
exports.createContentInstance = void 0;
const content_1 = require("./content");
/**
 *
 * @param contents
 * @returns handled contents
 */
exports.createContentInstance = (contents) => {
    const result = {};
    Object.keys(contents).forEach(key => {
        const keyStr = key;
        const list = contents[keyStr];
        if (!result[keyStr]) {
            result[keyStr] = [];
        }
        list.forEach(item => {
            var _a;
            (_a = result[keyStr]) === null || _a === void 0 ? void 0 : _a.push(new content_1.ContentDetailInstance(item));
        });
    });
    return result;
};
