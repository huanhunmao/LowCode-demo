import { MergeStructureNode } from '../../interface';
export declare const matchByExtend: <T extends MergeStructureNode>(record: Record<string, T>, list: T[], mergeNode: (base: T, current: T) => T) => void;
